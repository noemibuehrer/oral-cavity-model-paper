[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "ocmscripts"
version = "0.0.1"
description = "Modelling the tumour progression based on lymphatic spread patterns of newly diagnosed oral cavity squamous cell carcinoma patient records."
authors = [
    { name = "Roman Ludwig"},
    { name = "Noemi Bührer"},
]
readme = "README.md"
requires-python = "~=3.10"
dependencies = [
    "loguru",
    "python-dotenv",
    "tqdm",
    "typer",
    "dvc",
    "lydata",
    "lymph-model",
    "lyscripts>=1.0.1",
]

[tool.ruff]
line-length = 99
src = ["ocmscripts"]
include = ["pyproject.toml", "ocmscripts/**/*.py"]

[tool.ruff.lint]
extend-select = ["I"]  # Add import sorting

[tool.ruff.lint.isort]
known-first-party = ["ocmscripts"]
force-sort-within-sections = true

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
    "ruff>=0.13.0",
]
